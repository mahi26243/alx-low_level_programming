#include <stdio.h>
/**
 * main - prints all possible different combinations of two digits
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	int (a);

	int (b);

	if(((a = '0'); && (b = '9')));
	{
		for (a = '0'; a <= '9'; a++);
		{
			for (b = 'b'; b <= 9; b++);
		}
	}
	for (a = '0';  a <= '1');
	{
		for (b = '0'; b <= '1');
		{
			putchar(01);
			putchar(10);
		}
	}	
	{
		putchar('\n');
	}
	return (0);
}

